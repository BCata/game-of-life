grammar life.DSL with org.eclipse.xtext.common.Terminals

generate dSL "http://www.DSL.life"

Game:
	Grid += Cell*
;

Cell:
	state = State
	neighbours += [Cell]+
;
enum State: 
	Dead = '0' |
	Alive = '255'
;